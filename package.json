{
  "name": "electricity_xml_counter",
  "version": "1.0.0",
  "description": "Counts xml types in a given directory",
  "main": "app.js",
  "bin": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "toni",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.4.0",
    "libxmljs": "^0.19.5"
  }
}
